# Default paths (system-wide installs)
# $Id: Makefile.in,v 1.2 2000/12/09 22:52:42 dan_karrels Exp $
#

# Standard defines
CXX = @CXX@

SHLIB_CXX = @SHLIB_CXX@
SHLIB_LD = @SHLIB_LD@

DEFS = @DEFS@

CXXFLAGS = @CXXFLAGS@

BIN_PATH = @BIN_PATH@
LIB_PATH = @LIB_PATH@
LIBS = -L$(LIB_PATH) -lgnuworld @LIBS@

RM = @RM@ -f
CP = @CP@
MV = @MV@
LN = @LN@
AR = @AR@
RANLIB = @RANLIB@

############ You shouldn't change anything below this line ###################

SHELL = /bin/sh

SRCS = cservice.cc \
	sqlChannel.cc \
	sqlUser.cc
OBJS = $(patsubst %.cc, %.o, $(SRCS))
LIB = libcservice.so

all: $(OBJS) $(LIB)
everything: all
make: all

%.o: %.cc
	$(CXX) $(CXXFLAGS) -c $< -o $@

$(LIB): $(OBJS)
	$(SHLIB_LD) -o $(BIN_PATH)/GNUWorld $(OBJS) $(LIBS)

clean:
	$(RM) -f *.o $(BIN_PATH)/GNUWorld

distclean: clean

depend: $(SRCS)
	$(CXX) $(CXXFLAGS) -MM $(SRCS) >> Makefile

# DO NOT DELETE THIS LINE
