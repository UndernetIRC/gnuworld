# Server config file
# Configure server to connect to IRC uplink
# server on localhost:4400

uplink = 127.0.0.1
port = 4400
password = testnet
name = EUWorld2.test.net
description = EUWorld Undernet Service
numeric = 60
maxclients = 50

module = ccontrol.so euworld3.conf
#module = cservice.so cservice.conf
